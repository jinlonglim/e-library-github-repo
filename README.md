In app.py uncomment the code below and run to import books in MongoDB database.

# from .books import all_books

# # Insert all books from books.py into MongoDB if not already present
# Book.init_books(all_books)
